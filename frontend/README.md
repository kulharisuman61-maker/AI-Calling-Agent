# Frontend

React dashboard for monitoring system health, creating outbound calls, and
reviewing recent activity.

## Commands

```bash
npm install
npm start
npm run build
```

Set `REACT_APP_CALLING_API_URL` when the backend runs somewhere other than the
default local URL.
